## Bugs
- file-path and directory can be provided together
- cache never gets cleaned up
- folder names are not plurized

## Improvements
- clean file_cache on close
- consolidate logging config?
- consolidate file validation (present, is file etc, support ext)
- move suggested_name generation to it's own function
- categories need to be generalized
- cache all changes before applying
- build directory structure
- update existing directory structure
- move files to directory structure
- mutate existing directory structure
  - create categories
  - rename files and move to categories
  - delete empty directories
- use less tokens
  - use local LLM
- port to MacOS
  - build GUI

  
